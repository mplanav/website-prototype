<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="<%= traducciones.descripcionMeta || 'Restaurant El Brases - Disfruta de nuestra exquisita carta de platos.' %>" />
  <meta name="keywords" content="<%= traducciones.palabrasClaveMeta || 'restaurante, comida, platos, menú, especialidades' %>" />
  <meta name="author" content="Restaurant El Brases" />
  <title><%= title %></title>

  <!-- Estilos -->
  <link href="/styles.css" rel="stylesheet" />
  <link rel="icon" href="/images/favicon.ico" type="image/x-icon" />
</head>

<body class="bg-white text-gray-800 font-sans leading-relaxed antialiased">

  <!-- Fondo Hero (solo si se necesita) -->
  <div class="hero-bg fixed inset-0 -z-10 bg-cover bg-center bg-no-repeat opacity-70" style="background-image: url('/images/hero-bg.jpg');"></div>

  <!-- Navbar fija -->
  <nav class="bg-white border-b border-gray-200 shadow-md fixed top-0 left-0 w-full z-50">
    <div class="max-w-7xl mx-auto px-4 md:px-10 lg:px-20 py-4 flex items-center justify-between">
      <a href="<%= langPath || '/' %>" class="text-2xl font-bold text-indigo-700 hover:text-indigo-900 transition">
        Restaurant El Brases
      </a>
      <ul class="hidden md:flex space-x-6">
        <li><a href="#inicio" class="text-gray-700 hover:text-indigo-700 transition"><%= traducciones.menuInicio || 'Inicio' %></a></li>
        <li><a href="#especialidad" class="text-gray-700 hover:text-indigo-700 transition">Especialidad</a></li>
        <li><a href="#historia" class="text-gray-700 hover:text-indigo-700 transition">Historia</a></li>
        <li><a href="#carta" class="text-gray-700 hover:text-indigo-700 transition"><%= traducciones.menuCarta || 'Carta' %></a></li>
        <li><a href="#reserva" class="text-gray-700 hover:text-indigo-700 transition"><%= traducciones.menuReserva || 'Reserva' %></a></li>
        <li><a href="#contacto" class="text-gray-700 hover:text-indigo-700 transition"><%= traducciones.menuContacto || 'Contacto' %></a></li>
      </ul>
    </div>
  </nav>

  <!-- Contenido principal con padding-top para no quedar debajo de navbar -->
  <main class="pt-24 px-4 md:px-10 lg:px-20">
    <%- body %>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-100 text-center text-sm text-gray-500 py-6 border-t border-gray-200">
    &copy; <%= new Date().getFullYear() %> Restaurant El Brases. <%= traducciones.todosLosDerechos || 'Todos los derechos reservados.' %>
  </footer>

  <!-- Scroll suave para navegación interna -->
  <script>
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({ behavior: 'smooth' });
        }
      });
    });
  </script>

</body>
</html>